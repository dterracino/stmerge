# GPU installation with CUDA support
# Install with: pip install -r requirements-gpu.txt
#
# Prerequisites:
# - NVIDIA GPU with CUDA support
# - Check your CUDA version: nvidia-smi (see top-right corner)
# - This file uses CUDA 12.1 (compatible with CUDA 12.x)
#
# For other CUDA versions, install PyTorch manually first:
#   CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CUDA 12.4: pip install torch --index-url https://download.pytorch.org/whl/cu124
#   CUDA 12.8: pip install torch --index-url https://download.pytorch.org/whl/cu128
#   CUDA 13.0: pip install torch --index-url https://download.pytorch.org/whl/cu130
# Then: pip install -r requirements.txt

# PyTorch with CUDA 13.0 (current version for modern GPUs)
--extra-index-url https://download.pytorch.org/whl/cu130
torch>=2.0.0

# Include base dependencies
-r requirements.txt
